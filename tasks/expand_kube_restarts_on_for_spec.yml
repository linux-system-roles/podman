# SPDX-License-Identifier: MIT
# Generated by Cursor IDE using the models Composer 2.5, Codex 5.3, and Sonnet 4.6
---
# Expand restarts_on for one podman_kube_specs element.
# Input: __restarts_on_spec (one podman_kube_specs element)
- name: Resolve kube name for restarts_on
  include_tasks: kube_restart_paths.yml
  vars:
    __podman_restart_spec_item: "{{ __restarts_on_spec }}"

- name: Expand restarts_on for kube play spec
  include_tasks: expand_restarts_on.yml
  when:
    - __podman_spec_name | length > 0
    - __podman_kube_activate_systemd_unit | bool
