# SPDX-License-Identifier: GPL-3.0-only
---
- name: Ensure that the role runs with default parameters
  hosts: all
  vars:
    container_name: podman_default_container_name
  roles:
    - linux-system-roles.podman
