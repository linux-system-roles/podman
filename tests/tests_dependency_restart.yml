# SPDX-License-Identifier: MIT
# Generated by Cursor IDE using the models Composer 2.5, Codex 5.3, and Sonnet 4.6
---
- name: Test dependency restarts
  hosts: all
  tags:
    - tests::booted  # this test is for restarting services
  vars:
    podman_use_copr: false
    podman_fail_if_too_old: false
    __dep_file: /tmp/lsr_quadlet_dep_test.html
    __dep_kube_file: /tmp/lsr_kube_dep_test.html
    __dep_port: 18081
    __dep_kube_port: 18082
    __dep_image: docker.io/library/httpd:alpine
    __dep_content_v1: "<html><body>version1</body></html>"
    __dep_content_v2: "<html><body>version2</body></html>"
    __dep_kube_pod:
      apiVersion: v1
      kind: Pod
      metadata:
        name: lsr-dep-kube
      spec:
        containers:
          - name: lsr-dep-kube
            image: "{{ __dep_image }}"
            ports:
              - containerPort: 80
                hostPort: "{{ __dep_kube_port }}"
            volumeMounts:
              - mountPath: /usr/local/apache2/htdocs/index.html:Z
                name: www
                readOnly: true
        volumes:
          - name: www
            hostPath:
              path: "{{ __dep_kube_file }}"
    __dep_kube_absent:
      state: absent
      kube_file_content:
        apiVersion: v1
        kind: Pod
        metadata:
          name: lsr-dep-kube
        spec:
          containers:
            - name: lsr-dep-kube
              image: "{{ __dep_image }}"
    __dep_quadlet_container:
      name: lsr-dep-http
      type: container
      pull_image: false
      Install:
        WantedBy: default.target
      Container:
        Image: "{{ __dep_image }}"
        ContainerName: lsr-dep-http
        PublishPort: "{{ __dep_port }}:80"
        Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"
  tasks:
    - name: Test explicit restarts on dependency file
      block:
        - name: Run role - deploy dependency and container
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
                restarts: [lsr-dep-http]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Wait for httpd to serve content
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_v1
          until: __dep_http_v1.content is search('version1')
          retries: 15
          delay: 2

        - name: Run role - update dependency file
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
                restarts: [lsr-dep-http]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Verify container serves updated content
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_v2
          until: __dep_http_v2.content is search('version2')
          retries: 15
          delay: 2

        - name: Assert updated content
          assert:
            that: __dep_http_v2.content is search('version2')

      always:
        - name: Cleanup explicit restarts test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test explicit restarts on dependency file for kube play
      block:
        - name: Run role - create kube play dependency file
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v1 }}"

        - name: Run role - deploy kube play dependency and pod
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v1 }}"
                restarts: [lsr-dep-kube]
            podman_kube_specs:
              - state: started
                pull_image: false
                kube_file_content: "{{ __dep_kube_pod }}"

        - name: Wait for kube play httpd to serve content
          uri:
            url: "http://127.0.0.1:{{ __dep_kube_port }}/"
            return_content: true
          register: __dep_kube_http_v1
          until: __dep_kube_http_v1.content is search('version1')
          retries: 15
          delay: 2

        - name: Run role - update kube play dependency file
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v2 }}"
                restarts: [lsr-dep-kube]
            podman_kube_specs:
              - state: started
                pull_image: false
                kube_file_content: "{{ __dep_kube_pod }}"

        - name: Verify kube play serves updated content
          uri:
            url: "http://127.0.0.1:{{ __dep_kube_port }}/"
            return_content: true
          register: __dep_kube_http_v2
          until: __dep_kube_http_v2.content is search('version2')
          retries: 15
          delay: 2

        - name: Assert kube play updated content
          assert:
            that: __dep_kube_http_v2.content is search('version2')

      always:
        - name: Cleanup kube play restarts test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_kube_specs:
              - "{{ __dep_kube_absent }}"
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                state: absent

    - name: Test restarts_on explicit path for kube play
      block:
        - name: Run role - create kube play dependency file
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v1 }}"

        - name: Run role - deploy kube play with restarts_on
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v1 }}"
            podman_kube_specs:
              - state: started
                pull_image: false
                restarts_on:
                  - "{{ __dep_kube_file }}"
                kube_file_content: "{{ __dep_kube_pod }}"

        - name: Wait for kube play httpd to serve initial content
          uri:
            url: "http://127.0.0.1:{{ __dep_kube_port }}/"
            return_content: true
          register: __dep_kube_http_ron_v1
          until: __dep_kube_http_ron_v1.content is search('version1')
          retries: 15
          delay: 2

        - name: Run role - update kube play dependency via restarts_on
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                file_content: "{{ __dep_content_v2 }}"
            podman_kube_specs:
              - state: started
                pull_image: false
                restarts_on:
                  - "{{ __dep_kube_file }}"
                kube_file_content: "{{ __dep_kube_pod }}"

        - name: Verify kube play restarts_on path
          uri:
            url: "http://127.0.0.1:{{ __dep_kube_port }}/"
            return_content: true
          register: __dep_kube_http_ron_v2
          until: __dep_kube_http_ron_v2.content is search('version2')
          retries: 15
          delay: 2

      always:
        - name: Cleanup kube play restarts_on test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_kube_specs:
              - "{{ __dep_kube_absent }}"
            podman_quadlet_specs:
              - file: "{{ __dep_kube_file }}"
                state: absent

    - name: Test no restarts or restarts_on keeps stale container content
      block:
        - name: Run role - deploy without restart dependencies
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
              - "{{ __dep_quadlet_container }}"

        - name: Wait for httpd to serve initial content
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_no_restart_v1
          until: __dep_http_no_restart_v1.content is search('version1')
          retries: 15
          delay: 2

        - name: Run role - update dependency file without restart dependencies
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
              - "{{ __dep_quadlet_container }}"

        - name: Verify host file was updated
          slurp:
            path: "{{ __dep_file }}"
          register: __dep_file_on_host
          failed_when: __dep_file_on_host.content | b64decode is not search('version2')

        - name: Verify container still serves old content without restart
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_no_restart_stale
          until: __dep_http_no_restart_stale.content is search('version1')
          retries: 5
          delay: 1

        - name: Assert container did not pick up updated file without restart
          assert:
            that:
              - __dep_http_no_restart_stale.content is search('version1')
              - not __dep_http_no_restart_stale.content is search('version2')

      always:
        - name: Cleanup no restart dependencies test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test restarts ALL_SERVICES
      block:
        - name: Run role - ALL_SERVICES
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
                restarts: [ALL_SERVICES]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Update file with ALL_SERVICES
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
                restarts: [ALL_SERVICES]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Verify ALL_SERVICES restart
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_all
          until: __dep_http_all.content is search('version2')
          retries: 15
          delay: 2

      always:
        - name: Cleanup ALL_SERVICES test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test restarts_on explicit path
      block:
        - name: Run role - restarts_on path
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
              - name: lsr-dep-http
                type: container
                pull_image: false
                restarts_on:
                  - "{{ __dep_file }}"
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Update via restarts_on
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
              - name: lsr-dep-http
                type: container
                pull_image: false
                restarts_on:
                  - "{{ __dep_file }}"
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Verify restarts_on path
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_ron
          until: __dep_http_ron.content is search('version2')
          retries: 15
          delay: 2

      always:
        - name: Cleanup restarts_on test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test restarts_on ANY_DEPENDENCIES
      block:
        - name: Run role - ANY_DEPENDENCIES
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
              - name: lsr-dep-http
                type: container
                pull_image: false
                restarts_on: [ANY_DEPENDENCIES]
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Update via ANY_DEPENDENCIES
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
              - name: lsr-dep-http
                type: container
                pull_image: false
                restarts_on: [ANY_DEPENDENCIES]
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Verify ANY_DEPENDENCIES restart
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_any
          until: __dep_http_any.content is search('version2')
          retries: 15
          delay: 2

      always:
        - name: Cleanup ANY_DEPENDENCIES test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test dependency file removal triggers restart
      block:
        - name: Deploy for removal test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v1 }}"
                restarts: [lsr-dep-http]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Remove dependency file
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                state: absent
                restarts: [lsr-dep-http]

        - name: Verify dependency file removed
          stat:
            path: "{{ __dep_file }}"
          register: __dep_file_stat
          failed_when: __dep_file_stat.stat.exists

        - name: Verify service still active after dependency removal
          # noqa: command-instead-of-module
          command: systemctl is-active lsr-dep-http.service
          changed_when: false
          register: __dep_svc_active
          failed_when: __dep_svc_active.stdout != 'active'

      always:
        - name: Cleanup removal test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent

    - name: Test idempotency does not break running service
      block:
        - name: Deploy for idempotency test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
                restarts: [lsr-dep-http]
              - name: lsr-dep-http
                type: container
                pull_image: false
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Run role again with same configuration
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_quadlet_specs:
              - file: "{{ __dep_file }}"
                file_content: "{{ __dep_content_v2 }}"
                restarts: [lsr-dep-http]
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  Volume: "{{ __dep_file }}:/usr/local/apache2/htdocs/index.html:ro,Z"

        - name: Verify service still serves content after idempotent run
          uri:
            url: "http://127.0.0.1:{{ __dep_port }}/"
            return_content: true
          register: __dep_http_idem
          until: __dep_http_idem.content is search('version2')
          retries: 15
          delay: 2

      always:
        - name: Cleanup idempotency test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
              - file: "{{ __dep_file }}"
                state: absent

    - name: Test secret removal triggers restart
      vars:
        __dep_secret_name: lsr_dep_test_secret
        __dep_secret_value: "secret-v1"
      block:
        - name: Deploy container with secret
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            __sr_public: true
            podman_secrets:
              - name: "{{ __dep_secret_name }}"
                state: present
                data: "{{ __dep_secret_value }}"
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"
                  PodmanArgs: >-
                    --secret={{ __dep_secret_name }},type=env,target=DEP_TEST_SECRET

        - name: Remove secret and queue service restart
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_secrets:
              - name: "{{ __dep_secret_name }}"
                state: absent
                restarts: [lsr-dep-http]
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                pull_image: false
                activate_systemd_unit: false
                Install:
                  WantedBy: default.target
                Container:
                  Image: "{{ __dep_image }}"
                  ContainerName: lsr-dep-http
                  PublishPort: "{{ __dep_port }}:80"

        - name: Verify secret removed
          command: podman secret ls --format '{{ "{{" }}.Name{{ "}}" }}'
          register: __dep_secrets_list
          changed_when: false
          failed_when: __dep_secret_name in __dep_secrets_list.stdout_lines

        - name: Verify service still active after secret removal restart
          # noqa: command-instead-of-module
          command: systemctl is-active lsr-dep-http.service
          changed_when: false
          register: __dep_secret_svc_active
          failed_when: __dep_secret_svc_active.stdout != 'active'

      always:
        - name: Cleanup secret removal test
          include_tasks: tasks/run_role_with_clear_facts.yml
          vars:
            podman_quadlet_specs:
              - name: lsr-dep-http
                type: container
                state: absent
